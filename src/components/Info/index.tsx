import SearchIcon from '@mui/icons-material/Search';
const Info = () => {
  return (
    <p>
      <SearchIcon />
      It looks empty over here Try to type something!
    </p>
  );
};

export default Info;
